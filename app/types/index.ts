export interface CustomButtonProps {
  styling: string;
  lable: string;
  source?: string;
  h: string;
}
export interface CustomIconProp {
  styling: string;
}

export interface InernalCompnentProps {
  title: string;
  icon: string;
  description: string;
}

export interface MobMenuProp {
  CtrlStyles: string;
}
